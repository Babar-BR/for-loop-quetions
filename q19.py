# Row 1 → * - * - * -   (odd row  → starts with *)
# Row 2 → - * - * - *   (even row → starts with -)
# Row 3 → * - * - * -   (odd row  → starts with *)
# Row 4 → - * - * - *   (even row → starts with -)
# Row 5 → * - * - * -   (odd row  → starts with *)
# Row 6 → - * - * - *   (even row → starts with -)

# for i in range(1,7):
#     for j in range(1,7):
#         if (i+j)%2==0:
#             print("*",end="")
#         else:
#             print("-",end="")
#     print()